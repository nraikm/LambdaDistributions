"""Verification artifacts for the new sigma-MGF distribution families.

The top-level ``verification`` module carries the compact Spin and Pin
checks; each ``*_sigma_mgfs`` subpackage carries the exact backend and
pytest regression suite for one anthology family.
"""
