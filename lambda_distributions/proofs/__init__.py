"""Proof and verification packages for the proved matrix-group families.

Each family keeps its executable verification backend, regression
suite, README, and (for now) the standalone proof tex/pdf.  The
interactive notebooks live in the repository's ``marimo_notebooks``
package; the consolidated proofs live in ``proved_matrix_groups``.
"""
